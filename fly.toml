app = "65dza165d16za6"
primary_region = "cdg"

# VM configuration
[vm]
cpu_kind = "shared"
cpus = 1
memory = "2gb"

# Your bot process - ONLY ONE!
[processes]
app = "npm start"

# ADD THIS to prevent multiple machines:
[deploy]
max_per_region = 1  # Maximum 1 machine per region
